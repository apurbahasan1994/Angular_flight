import { FlightTypePipe } from './flight-type.pipe';

describe('FlightTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FlightTypePipe();
    expect(pipe).toBeTruthy();
  });
});
