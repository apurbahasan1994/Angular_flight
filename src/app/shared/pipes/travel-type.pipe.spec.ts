import { TravelTypePipe } from './travel-type.pipe';

describe('TravelTypePipe', () => {
  it('create an instance', () => {
    const pipe = new TravelTypePipe();
    expect(pipe).toBeTruthy();
  });
});
